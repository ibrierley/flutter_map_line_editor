name: flutter_map_line_editor
description: Line editor class for flutter_map
version: 4.0.3
repository: https://github.com/ibrierley/flutter_map_line_editor

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  latlong2: ">=0.8.0 <0.9.0"
  # TODO use pub.dev version once released
  flutter_map_dragmarker:
    git:
      url: https://github.com/josxha/flutter_map_dragmarker.git
      ref: master

dev_dependencies:
  flutter_lints: ">=1.0.4"
  flutter_test:
    sdk: flutter
