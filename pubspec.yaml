name: flutter_map_line_editor
description: Line editor class for flutter_map
version: 1.0.0+1
repository: https://github.com/ibrierley/flutter_map_line_editor

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_map: ^1.0.0
  flutter_map_dragmarker:
    git:
      url: https://github.com/pablojimpas/flutter_map_dragmarker
      ref: bump-flutter_map-1.0.0
  latlong2: ^0.8.0

dev_dependencies:
  flutter_lints: ">=1.0.4"
  flutter_test:
    sdk: flutter
